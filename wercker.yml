box: nodesource/node
build:
  steps:
    - script:
        code: export NODE_ENV=development
    - npm-install
    - samverschueren/tsd-install
    - hgen/gulp:
        tasks: build

deploy:
  steps:
    - ematsen/gh-pages:
        token: $GITHUB_TOKEN
        path: dist