box: nodesource/node
build:
  steps:
    - script:
        code: export NODE_ENV=development
    - npm-install
    - samverschueren/tsd-install
    - hgen/gulp:
        tasks: build

deploy:
  steps:
    - matsen/gh-pages:
        token: $GITHUB_TOKEN
        path: dist