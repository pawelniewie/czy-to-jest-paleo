box: nodesource/node
build:
  steps:
    - npm-install
    - samverschueren/tsd-install
    - hgen/gulp:
        tasks: build

deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        path: dist